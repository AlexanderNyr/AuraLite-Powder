# Fonts

This directory would contain embedded or loaded UI font assets.

For MVP, we use default egui fonts and system fonts. Future: add custom fonts for nuclear symbols, etc.

Place .ttf files here and load via `include_bytes!` or asset loader.
